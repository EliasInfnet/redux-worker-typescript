export enum GraphicsTaskStatus {
  IDLE = 'IDLE',
  PAUSED = 'PAUSED',
  EXECUTING = 'EXECUTING',
  FINISHED = 'FINISHED'
}

export enum GraphicsInspectionCriticality {
  IGNORE = 'IGNORE',
  LOW = 'LOW',
  MEDIUM = 'MEDIUM',
  HIGH = 'HIGH'
}
