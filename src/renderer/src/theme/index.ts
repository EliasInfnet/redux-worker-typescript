import { MantineColorScheme, MantineTheme } from '@mantine/core'

const theme: Partial<MantineTheme> = {
  primaryColor: 'orange'
}

const colorScheme: Partial<MantineColorScheme> = 'dark'

export { theme, colorScheme }
